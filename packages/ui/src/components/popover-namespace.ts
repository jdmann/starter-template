import { PopoverRoot } from "@ark-ui/react";
import { PopoverContent, PopoverPortal, PopoverTrigger } from "./popover";

export const Popover = {
  Root: PopoverRoot,
  Trigger: PopoverTrigger,
  Portal: PopoverPortal,
  Content: PopoverContent,
};
